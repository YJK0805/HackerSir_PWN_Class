service format_string2
{
    disable = no
    type = UNLISTED
    socket_type = stream
    protocol = tcp
    server = /home/format_string2/run.sh
    user = format_string2
    port = 10005
    flags = REUSE
    bind = 0.0.0.0
    wait = no
}

